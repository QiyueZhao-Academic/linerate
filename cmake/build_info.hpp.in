// build_info.hpp — generated by CMake. Do not edit.
//
// Provenance is baked into the binary and copied into every dataset, so that a
// number written today is still interpretable a year from now: which commit,
// which compiler, which flags.
#pragma once

#define LR_GIT_COMMIT "@LR_GIT_COMMIT@"
#define LR_COMPILER   "@LR_COMPILER@"
#define LR_CXX_FLAGS  "@LR_CXX_FLAGS@"
#define LR_BUILD_TYPE "@LR_BUILD_TYPE@"
#define LR_VERSION    "@LR_VERSION@"
